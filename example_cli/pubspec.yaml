name: libsignal_example_cli
description: CLI example demonstrating libsignal library usage
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.10.0

dependencies:
  libsignal:
    path: ../

dev_dependencies:
  lints: ^6.0.0
